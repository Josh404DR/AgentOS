# 2026-08-10 決策記錄：四個待決項目

decided_at: 2026-08-10 Asia/Taipei
decided_by: Claude（Josh 明確授權判斷，原話：「幫我做決定」，針對本文件列出的 4 項）
governance_version: 1.4.0
定位: 這是 Claude 在 Josh 明確授權下做的判斷紀錄，不是 Josh 本人逐項核准；核准動作本身（如 1424 的 Risky 工單）仍需 Josh 用他自己的身分執行，我不能代替他按下核准。

---

## 1. `telegram-1424`（Dashboard 救援請求）核准

**決定**：不代為核准（Risky 工單的核准權限本來就限定 Josh 本人身分，這條界線不因「你決定」而變動）。改為提供可直接使用的核准措辭，避開 1427 踩到的 bug。

**建議措辭**（單行寫法，避免條列清單跨行）：
> 我核准此 Risky 工單執行。dispatch_id: telegram-telegram-1449022024-1424-...（禁止安裝或移除軟體、禁止 production_data 寫入、僅限最小化遠端救援腳本，其餘按 TASK.md 原定範圍）

重點：把原本會拆成條列的「明確禁止」項目全部收進同一行、同一子句內，這樣 `classify_task.ps1` 的否定詞窗口（`[^\r\n，。；;]{0,24}`）才吃得到。

**動作**：Josh 用這段（或類似的單行版本）透過 Telegram/dashboard 正式核准流程送出。

---

## 2. 分類器 bug（否定詞不跨行）修復

**決定**：APPROVE，轉正式 dispatch，標記 Risky（動的是核准判斷邏輯本身）。

**理由**：根因已查明並實測重現（`classify_task.ps1` 第25-37行否定詞窗口不支援跨行），修法明確且範圍小（改一段正則 + 補兩個回歸測試案例），不是模糊不清的整改。技術方案已經寫在 `docs\plans\2026-08-10-escalation-classifier-1424-1427-misjudgment.md` 的「需要修的範圍」章節，可以直接轉成 TASK.md 派工。

**動作**：下次跟 code 執行端交接時，把該草稿轉成正式 TASK.md dispatch，走 Codex Plan/Builder/Verify 標準流程。不需要 Josh 逐字重新描述問題，草稿本身已經是可派工的規格。

---

## 3. 項次 7、8（P-1 Verify isolation、P-3 歷史資料信任分類）

**決定**：現在就轉成正式 AgentOS 派工的 TASK.md 規格（不是今天在聊天室直接改檔案），排入佇列背景執行，不用等其他項目做完（沿用 Josh 08-10 稍早已裁決的「可並行」）。

**理由**：這兩項規模足夠大、涉及系統結構性保證（Verify 獨立性）與歷史資料判讀，適合走正式 Codex Plan 拆解與獨立 Verify，而不是聊天室裡零散修改。P-3 額外要注意：08-10 escalation 盤點發現 `learning-candidate` dedupe bug 的重複記錄延伸到 07-26，超出 `50_LESSONS.md` 記載的 07-19~07-21 污染區間，正式派工時的 Current-State Audit 步驟要把這個範圍重新核實進去，不能照舊用 07-21 當截止點。

**動作**：任務看板 §3 的 P-1/P-3 列已標記「暫緩走正式派工」；現在解除暫緩，下次跟 code 執行端交接時一併開票。

---

## 4. ~46 支核心角色/治理檔案稽核

**決定**：排定明天（2026-08-11）開始，不是無限期「另開一輪」。

**理由**：這項風險較高（動的是權限邊界定義），一直用「另開一輪」帶過等於沒有排程，跟今天 AGENTS.md 新增的收尾稽核義務精神相違——已知未結案項目不能沒有明確的下一步時間點。今天的 Josh-only 三項 + 分類器/P-1/P-3 派工已經排滿今天的合理工作量，不需要今天硬塞。

**動作**：任務看板 §3 標記「排定 2026-08-11 開始」，比照 08-08 那次「先看 diff、查工單、無工單先問」的流程執行。

---

## 總結：今天結束時的狀態

- 1424：等 Josh 用上方建議措辭核准（Josh-only，唯一真正需要 Josh 親自動作的項目）。
- 分類器 bug、P-1、P-3：APPROVE 轉正式派工，下次交接開票，不需要 Josh 再做決定。
- ~46 支核心檔案稽核：排定明天，不是今天，也不是無限期延後。
